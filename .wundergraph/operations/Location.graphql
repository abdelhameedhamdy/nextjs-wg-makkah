query {
  getVehicleActivity {
    body {
      monitoredVehicleJourney {
        originShortName
        bearing
        speed
        vehicleLocation {
          latitude
          longitude
        }
        directionRef
        operatorRef
        vehicleRef
      }
    }
  }
}
